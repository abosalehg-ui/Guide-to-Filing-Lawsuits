# مراجعة هندسية شاملة — Guide-to-Filing-Lawsuits

**تاريخ المراجعة:** 2026-08-03
**المراجَع:** الفرع `claude/senior-repo-review-dl41kq` عند الـ commit رقم `69432a7`
**تصنيف المشروع:** أداة عرض محتوى ثابتة من ملف واحد، بلا backend وبلا تخزين بيانات مستخدم — **الملف التعريفي C**

---

## الحكم النهائي

مشروع من ملف `index.html` واحد بحجم 1486 سطراً يعرض دليلاً مرجعياً لتصنيفات الدعاوى في منصة ناجز. التنفيذ البصري أنظف من المتوسط لمشروع بهذا الحجم — هناك design tokens حقيقية و responsive breakpoints مدروسة — لكن الطبقة الوظيفية تحتها هشّة: البيانات والعرض والسلوك مخلوطة في ملف واحد، وأعداد الخدمات المعروضة للمستخدم مكتوبة يدوياً في HTML وقد انفصلت فعلياً عن البيانات، و 99 عنصراً من أصل 319 غير قابلة للبحث إطلاقاً، والتطبيق بكامله غير قابل للتشغيل بلوحة المفاتيح.

**الدرجة الإجمالية المرجّحة: 5.2 / 10** (الملف التعريفي C: هندسة 40% / UX 40% / أمن 20%)

**هل المشروع جاهز للاستخدام/النشر؟ — بشروط.** هو منشور ويعمل فعلاً كمرجع للقراءة، ولا يحمل ثغرة أمنية قابلة للاستغلال. لكن إصلاح ثلاثة أمور شرط قبل اعتباره منتجاً: إتاحة التنقل بلوحة المفاتيح، تصحيح الأعداد المضلّلة، وإضافة إخلاء مسؤولية يوضح أن المحتوى غير رسمي.

**أهم ثلاث مشاكل:**
1. أعداد الخدمات في البطاقات مكتوبة يدوياً ومخالفة للبيانات الفعلية في 8 مواضع (بطاقة تقول «25+ خدمة» وخلفها 11 عنصراً فقط) — محتوى مضلّل في دليل قانوني.
2. البحث لا يغطي قسم «الخدمات الإلكترونية» بالكامل (99 عنصراً)، ويفشل على أي كتابة عربية بدون همزة: `احوال` تعطي 0 نتيجة بينما `أحوال` تعطي 37.
3. كل عناصر التفاعل عبارة عن `<div onclick=...>` بلا `tabindex` ولا `role`، ولا يوجد `:focus` واحد في الملف — التطبيق غير قابل للاستخدام بلوحة المفاتيح أو بقارئ الشاشة.

---

## 1. نظرة عامة

**ما يفعله المشروع:** صفحة واحدة تعرض شجرة تصنيفات الدعاوى القضائية والإنهاءات والخدمات الإلكترونية في منصة ناجز، مع تبويبات تنقّل، ونافذة modal للتفاصيل، وبحث نصي فوري. لا يوجد تفاعل مع أي API، والمحتوى كله مكتوب مباشرة داخل الملف.

**المكوّنات:**

| البند | القيمة |
|---|---|
| عدد الملفات (خارج `.git`) | 2 |
| `index.html` | 1486 سطراً / 66470 بايت |
| `README.md` | 143 سطراً / 6028 بايت |
| CSS (داخل `<style>`) | ≈ 840 سطراً |
| JavaScript (داخل `<script>`) | ≈ 330 سطراً |
| اعتماديات runtime | لا يوجد أي إطار أو مكتبة JS |

**الـ stack المستخدم فعلياً — مأخوذ من الملف نفسه لا من README:**

- HTML5 + CSS3 + Vanilla JavaScript، بلا build step وبلا package manager.
- خط واحد خارجي محمّل من `fonts.googleapis.com`:

```html
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;800&family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
```

هذا هو الاعتماد الخارجي الوحيد في المشروع بأكمله.

**ما تم التحقق من غيابه (فحص مباشر للمستودع):**

| العنصر | الحالة |
|---|---|
| اختبارات | غير موجودة — لا مجلد `tests/` ولا أي ملف اختبار |
| CI/CD | غير موجود — لا مجلد `.github/` إطلاقاً |
| linter / formatter | غير موجود |
| `.gitignore` | غير موجود |
| `.env.example` | لا ينطبق (لا توجد أسرار) |
| `LICENSE` | **غير موجود** رغم أن README يذكر ترخيصاً |
| `README.md` | موجود |

**حجم البيانات الفعلي (محسوب برمجياً من الملف):**

| المجموعة | عدد التصنيفات الفرعية | عدد العناصر |
|---|---|---|
| `lawsuitCategories` | 34 | 191 |
| `completionData` | 6 | 29 |
| `serviceData` | 10 | 99 |
| **الإجمالي** | | **319** |

---

## 2. المراجعة التقنية والهندسية

### 2.1 المعمارية وفصل الاهتمامات — 3/10

كل شيء في ملف واحد: البيانات، الأنماط، القوالب، ومعالجات الأحداث. هذا مقبول كقرار توزيع (ملف واحد يُفتح بالنقر المزدوج)، لكن التنفيذ لم يفصل الطبقات حتى داخل الملف. أوضح مثال: `serviceData` — وهي ثلث بيانات المشروع — معرّفة **داخل** دالة العرض، لا بجوار مجموعتي البيانات الأخريين:

`index.html` — الدالة `showServiceCategory`:

```js
function showServiceCategory(key) {
    const serviceData = {
        'social-status': {
            title: 'الحالات الاجتماعية',
            items: ['تصديق عقد زواج', 'توثيق الاستحقاق لتمويل الزواج', ...]
        },
        ...
    };

    const data = serviceData[key];
```

بينما `lawsuitCategories` و `completionData` معرّفتان على مستوى الوحدة. النتيجة ليست أسلوبية فقط — سنتناول أثرها الوظيفي في 2.4.

**التقييم:** `مؤكد`. البنية ليست غير قابلة للإصلاح، لكنها تجعل أي تعديل على البيانات عملية بحث نصي داخل ملف من 1486 سطراً.

**الإصلاح المقترح — نقل البيانات إلى نطاق الوحدة:**

```js
// قديم
function showServiceCategory(key) {
    const serviceData = { 'social-status': {...}, ... };
    const data = serviceData[key];
```

```js
// جديد — بجوار lawsuitCategories و completionData
const serviceData = { 'social-status': {...}, ... };

function showServiceCategory(key) {
    const data = serviceData[key];
```

---

### 2.2 القابلية للقراءة والصيانة — 5/10

التنسيق منتظم والمسافات البادئة متسقة وأسماء الدوال إنجليزية واضحة (`showSubcategory`, `performSearch`). لكن ثلاثة أنماط تُضعف الصيانة:

**أولاً: الأنماط تُكتب من JavaScript بدل CSS class.** في `showSubcategory`:

```js
const subCatHeader = document.createElement('div');
subCatHeader.style.cssText = 'background: var(--bg-cream); padding: 1rem 1.5rem; font-weight: 600; color: var(--primary); border-bottom: 2px solid var(--primary-light);';
```

هذا يتجاوز طبقة CSS بالكامل رغم وجود نظام tokens منظّم في `:root`.

**ثانياً: خلط `addEventListener` مع `onclick` المضمّن.** المشروع يستخدم الأسلوبين معاً — 22 مثيلاً لـ `onclick=` مضمّناً في HTML، بينما تبويبات التنقّل تُربط بالطريقة الحديثة:

```html
<div class="category-card" onclick="showCompletionDetails('heirs')">
```

```js
document.querySelectorAll('.nav-tab').forEach(tab => {
    tab.addEventListener('click', () => { ... });
});
```

**ثالثاً: 12 سمة `style="..."` مضمّنة** في HTML، منها عناوين الأقسام:

```html
<h2 style="font-family: 'Cairo', sans-serif; margin-bottom: 1.5rem; color: var(--primary);">الخدمات الإلكترونية</h2>
```

**التقييم:** `مؤكد`.

---

### 2.3 التكرار و DRY وروائح الكود — 4/10

**تكرار بنيوي واضح:** ثلاث دوال تبني نفس نافذة الـ modal بنفس الطريقة تقريباً. `showCompletionDetails`:

```js
document.getElementById('modal-title').textContent = data.title;
let itemsHtml = data.items.map(item => `<li>${item}</li>`).join('');
document.getElementById('modal-body').innerHTML = `
    <p><strong>الخدمات المتاحة في هذا القسم:</strong></p>
    <ul>${itemsHtml}</ul>
`;
document.getElementById('modal-overlay').classList.add('active');
```

و `showServiceCategory` — نفس الكود حرفياً باستثناء نص واحد:

```js
document.getElementById('modal-title').textContent = data.title;
let itemsHtml = data.items.map(item => `<li>${item}</li>`).join('');
document.getElementById('modal-body').innerHTML = `
    <p><strong>الخدمات المتاحة:</strong></p>
    <ul>${itemsHtml}</ul>
`;
document.getElementById('modal-overlay').classList.add('active');
```

**دالة وسيطة بلا وظيفة:**

```js
function showSearchResultDetail(name, subcategory, category) {
    showCaseTypeDetails(name, subcategory, category);
}
```

تمرير خالص لا يضيف شيئاً، ووجوده يجبر البحث على تمرير الوسائط عبر سلسلة نصية في `onclick` (انظر 4.2).

**الإصلاح المقترح — دالة modal واحدة:**

```js
// قديم — دالتان متطابقتان
function showCompletionDetails(key) { /* ...البناء اليدوي... */ }
function showServiceCategory(key)   { /* ...نفس البناء... */ }
```

```js
// جديد
function openListModal(data, intro) {
    if (!data) return;
    modalTitle.textContent = data.title;
    modalBody.replaceChildren(
        el('p', { html: `<strong>${intro}</strong>` }),
        el('ul', { children: data.items.map(t => el('li', { text: t })) })
    );
    modalOverlay.classList.add('active');
}
const showCompletionDetails = k => openListModal(completionData[k], 'الخدمات المتاحة في هذا القسم:');
const showServiceCategory   = k => openListModal(serviceData[k],   'الخدمات المتاحة:');
```

**التقييم:** `مؤكد`.

---

### 2.4 معالجة الأخطاء والحالات الحدّية — 4/10

**أخطر مشكلة في المشروع: فهرس البحث لا يشمل الخدمات الإلكترونية.** بناء الفهرس يمرّ على مجموعتين فقط:

```js
const allSearchableItems = [];

Object.keys(lawsuitCategories).forEach(catKey => { ... });

Object.keys(completionData).forEach(key => { ... });
```

ولأن `serviceData` محبوسة داخل `showServiceCategory` (2.1)، فهي غير مرئية لهذا الكود أصلاً. النتيجة المحسوبة: **الفهرس يحتوي 220 عنصراً من أصل 319 — أي 99 عنصراً (31%) غير قابلة للعثور عليها بالبحث**، بينما README يَعِد صراحةً بـ «البحث السريع في جميع أنواع الدعاوى **والخدمات**».

**ثانياً: أعداد مكتوبة يدوياً انفصلت عن البيانات.** البطاقات تحمل أعداداً ثابتة في HTML:

```html
<div class="category-card" data-category="general">
    ...
    <span class="category-count">7 تصنيفات فرعية</span>
</div>
```

بينما `lawsuitCategories.general.subcategories` تحتوي 6 مفاتيح فقط (`أخرى`, `الدعاوي المستعجلة`, `عقارية`, `مالية`, `مروري`, `الاستئناف`). والانحراف أوسع بكثير في قسم الخدمات:

| البطاقة | العدد المعلن | العدد الفعلي في `serviceData` |
|---|---|---|
| الدعاوى العامة | 7 تصنيفات فرعية | 6 |
| الحالات الاجتماعية | 15 خدمة | 13 |
| الرهون والعقارات | 20+ خدمة | 14 |
| القضاء | 18 خدمة | 14 |
| التنفيذ | 25+ خدمة | 11 |
| التراخيص | 15+ خدمة | 9 |
| الوكالات والإقرارات | 20 خدمة | 11 |
| كتابة العدل الافتراضية | 20+ خدمة | 9 |

في دليل قانوني، رقم يقول «25+» ثم يفتح قائمة من 11 بنداً ليس خطأ تجميلياً — هو ادّعاء غير صحيح أمام المستخدم.

**ثالثاً: فشل صامت.** ثلاث دوال تنتهي بلا أي إشعار عند عدم وجود البيانات:

```js
const data = completionData[key];
if (!data) return;
```

لا رسالة، ولا سجلّ، ولا حالة بديلة — نقرة المستخدم تختفي بلا أثر.

**الإصلاح المقترح — اشتقاق الأعداد بدل كتابتها:**

```html
<!-- قديم -->
<span class="category-count">7 تصنيفات فرعية</span>
```

```html
<!-- جديد: يُملأ من البيانات -->
<span class="category-count" data-count-for="general"></span>
```

```js
// جديد — يُنفَّذ مرة واحدة عند التحميل
document.querySelectorAll('[data-count-for]').forEach(el => {
    const n = Object.keys(lawsuitCategories[el.dataset.countFor].subcategories).length;
    el.textContent = `${n} تصنيفات فرعية`;
});
```

**الإصلاح المقترح — ضمّ الخدمات إلى الفهرس:**

```js
// جديد — يُضاف بعد نقل serviceData إلى نطاق الوحدة
Object.keys(serviceData).forEach(key => {
    serviceData[key].items.forEach(item => {
        allSearchableItems.push({
            name: item, category: serviceData[key].title,
            subcategory: 'خدمة إلكترونية', type: 'خدمة'
        });
    });
});
```

**التقييم:** `مؤكد` — تم التحقق من الأعداد بتنفيذ البيانات برمجياً.

---

### 2.5 الأداء واستهلاك الموارد — 7/10

هذه أقوى نقاط المشروع هندسياً، وتستحق ذكرها بدقة: صفحة واحدة بلا أي مكتبة JS، بلا build، وبلا طلبات شبكة بعد التحميل الأول. البيانات كلها في الذاكرة، والبحث الخطي على 220 عنصراً يُنفَّذ في أقل من ميلي ثانية — لذا غياب الـ `debounce` على `oninput` **ليس** مشكلة أداء هنا ولا أُدرجه كملاحظة.

ملاحظتان حقيقيتان:

**أولاً: غياب `preconnect` لخط Google.** الرابط الوحيد الخارجي يُكتشف في وقت متأخر من تحليل الـ `<head>`، ما يضيف دورة DNS + TLS كاملة قبل ظهور النص:

```html
<link href="https://fonts.googleapis.com/css2?family=Tajawal:..." rel="stylesheet">
```

**ثانياً:** إعادة إنشاء كائن `serviceData` عند كل نقرة (2.1) — أثرها الفعلي مهمَل، وأذكرها للاكتمال لا كمشكلة أداء.

**الإصلاح:**

```html
<!-- قديم -->
<link href="https://fonts.googleapis.com/css2?family=Tajawal:..." rel="stylesheet">
```

```html
<!-- جديد -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Tajawal:..." rel="stylesheet">
```

**التقييم:** `مؤكد`.

---

### 2.6 الاختبارات والتغطية — 0/10

**لا توجد اختبارات على الإطلاق.** لا مجلد اختبارات، ولا أداة، ولا حتى فحص HTML آلي. تم التأكد بجرد المستودع كاملاً: ملفان فقط، `index.html` و `README.md`.

هذا مباشرةً هو سبب بقاء انحرافات الأعداد في 2.4 دون اكتشاف: اختبار واحد يقارن عدد `.category-count` بعدد مفاتيح البيانات كان سيلتقط ثمانية أخطاء دفعة واحدة.

**التقييم:** `مؤكد`. الدرجة 0 بموجب مرساة التقييم، لا تشدداً.

---

### 2.7 التوثيق — 5/10

README مكتوب جيداً من منظور المستخدم النهائي: نظرة عامة، جدول مميزات، فهرس محتوى، وخطوات تشغيل صحيحة. لكن فيه أربعة عيوب حقيقية:

**أولاً: قسم لقطات شاشة بلا لقطات شاشة** — جدول يحتوي أوصافاً نصية فقط:

```markdown
| الصفحة الرئيسية | البحث |
|:---------------:|:-----:|
| عرض التصنيفات الرئيسية للدعاوى | البحث الفوري في الخدمات |
```

**ثانياً: ترخيص مذكور بلا ملف ترخيص.** README يقول:

```markdown
هذا المشروع متاح للاستخدام الحر.
```

ولا يوجد ملف `LICENSE` في المستودع. قانونياً، غياب الترخيص يعني «جميع الحقوق محفوظة» افتراضياً — وهو ما يتعارض مباشرةً مع دعوة المساهمة في نفس الملف («المساهمات مرحب بها!»).

**ثالثاً: نسبة العمل متضاربة داخل الملف الواحد.** يظهر ثلاثة إسنادات مختلفة: «إعداد وتنظيم: فيصل بن عائض بن سالم المزيني»، ثم قسم «👤 المطور» باسم «عبدالكريم العبود»، ثم «شكر خاص للزميل فيصل عائض المزيني على إعداده هذا المشروع». القارئ لا يستطيع تحديد من كتب الكود ومن أعدّ المحتوى.

**رابعاً: صفر تعليقات في JavaScript.** التعليقات الموجودة في الملف كلها فواصل أقسام CSS/HTML (`/* Header */`, `<!-- Modal -->`)، ولا يوجد أي شرح لبنية البيانات أو لمصدر المحتوى القانوني أو لتاريخ آخر مطابقة له مع ناجز.

**التقييم:** `مؤكد`.

---

### 2.8 اصطلاحات التسمية والاتساق الأسلوبي — 6/10

الاصطلاح العام سليم ومتّبع: `camelCase` للدوال والمتغيّرات، `kebab-case` لأسماء CSS classes و `id`، وأسماء إنجليزية للكود مع بيانات عربية — فصل صحيح.

الخصم لثلاثة أسباب: خلط أسلوبَي ربط الأحداث (2.2)، واستخدام `style` مضمّن بجانب نظام tokens منظّم (2.2)، وتضارب رسم الكلمة العربية داخل البيانات نفسها — `الدعاوى العامة` بالألف المقصورة مقابل `الدعاوي المستعجلة` و `دعاوي الزواج والفرقة` بالياء. هذا التضارب له أثر وظيفي مباشر على البحث، وأتناوله في 3.4.

**التقييم:** `مؤكد`.

---

### متوسط الفئة الهندسية

`(3 + 5 + 4 + 4 + 7 + 0 + 5 + 6) ÷ 8 = 34 ÷ 8 =` **4.3 / 10**

---

## 3. المظهر والتصميم وتجربة المستخدم

### 3.1 الاتساق البصري — 7/10

**هذا أفضل ما في المشروع، وأذكر سبب التقدير تحديداً:** يوجد نظام design tokens حقيقي ومستخدَم فعلاً في كامل الملف، لا مجرّد متغيّرات مزخرفة:

```css
:root {
    --primary: #1B5E20;
    --primary-light: #4CAF50;
    --primary-dark: #0D3311;
    --secondary: #FFC107;
    --bg-cream: #FAFAF5;
    --shadow-soft: 0 4px 20px rgba(0,0,0,0.08);
    --radius: 16px;
    --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
```

نصف قطر واحد، ظلّان معرّفان مركزياً، منحنى انتقال واحد، وتدرّج ألوان الأيقونات منظّم في ست فئات متجانسة (`icon-blue` … `icon-teal`). هذا انضباط أعلى من المعتاد في مشاريع الملف الواحد.

الخصم بسبب الـ 12 سمة `style` المضمّنة و `cssText` من JavaScript (2.2) التي تتجاوز هذا النظام في المواضع التي كانت ستستفيد منه أكثر.

**التقييم:** `مؤكد`.

---

### 3.2 وضوح الاستخدام والتنقّل — 5/10

**أولاً: لا توجد روابط خارجية إطلاقاً.** فحص الملف يُظهر **صفر** وسم `<a>`. دليل كامل عن منصة ناجز لا يحتوي رابطاً واحداً إلى ناجز — الخطوة الأولى تذكر العنوان كنص عادي غير قابل للنقر:

```html
<p>قم بزيارة موقع ناجز (najiz.sa) وسجل الدخول باستخدام حسابك في النفاذ الوطني الموحد</p>
```

**ثانياً: محتوى الـ modal لكل الدعاوى الـ 191 متطابق.** الدالة `showCaseTypeDetails` تُنتج نفس الخطوات الخمس العامة أياً كان نوع الدعوى:

```js
document.getElementById('modal-body').innerHTML = `
    <p><strong>التصنيف الرئيسي:</strong> ${mainCategory}</p>
    <p><strong>التصنيف الفرعي:</strong> ${subCategory}</p>
    <p style="margin-top: 1rem;"><strong>للتقديم على هذه الدعوى:</strong></p>
    <ul>
        <li>قم بتسجيل الدخول في نظام ناجز</li>
        ...
```

لا مستندات مطلوبة، ولا محكمة مختصة، ولا رسوم، ولا مدد. الوعد في README بـ «شرح تفصيلي لخطوات تقديم الدعوى» لا يقابله محتوى في الكود.

**ثالثاً: لا توجد حالة قابلة للمشاركة.** لا `hash routing` ولا تعامل مع `history` — لا يمكن إرسال رابط يفتح تصنيفاً معيّناً، وإعادة تحميل الصفحة تعيد المستخدم دائماً إلى التبويب الأول. لمرجع من هذا النوع، هذه خسارة وظيفية كبيرة.

**رابعاً: الطباعة تُنتج تبويباً واحداً.** أنماط الطباعة تخفي التنقّل فقط:

```css
@media print {
    .nav-container, .search-container, .back-btn {
        display: none;
    }
}
```

لكن الأقسام غير النشطة تبقى `display: none` من `.section`، فالمستخدم الذي يطبع الدليل يحصل على القسم المفتوح فقط — لا على الدليل.

**الإصلاح المقترح:**

```css
/* قديم */
@media print {
    .nav-container, .search-container, .back-btn { display: none; }
}
```

```css
/* جديد */
@media print {
    .nav-container, .search-container, .back-btn, .modal-overlay { display: none !important; }
    .section { display: block !important; }
    #categories-view, #subcategory-view { display: block !important; }
}
```

**التقييم:** `مؤكد`.

---

### 3.3 الاستجابة عبر المقاسات — 7/10

معالجة مدروسة وأوسع من المتوقع: ثلاث نقاط توقّف (`768px`, `380px`)، واستهداف منفصل لأجهزة اللمس، ودعم صريح للشاشات ذات النتوء:

```css
@media (hover: none) and (pointer: coarse) {
    .category-card:active { transform: scale(0.98); }
    .case-type-arrow { opacity: 0.5; }
}

@supports (padding: max(0px)) {
    .header { padding-top: max(2rem, env(safe-area-inset-top)); }
    .nav-container {
        padding-left: env(safe-area-inset-left);
        padding-right: env(safe-area-inset-right);
    }
}
```

كشف سهم العنصر على أجهزة اللمس (`opacity: 0.5`) تعويضاً عن غياب `:hover` تفصيلة صحيحة قلّما تُنفَّذ. الشبكات تستخدم `auto-fill` مع `minmax` بدل استعلامات صلبة، و `clamp()` لحجم العنوان.

الخصم الوحيد: `.nav-container` يستخدم `padding-left/right` بدل `padding-inline-start/end`، وهو ملف RTL كامل — يُناقَش في 3.4.

**التقييم:** `مؤكد`.

---

### 3.4 دعم العربية و RTL — 6/10

**ما هو صحيح:** `<html lang="ar" dir="rtl">` معلن بشكل سليم في الجذر، والأرقام الغربية (0-9) مستخدمة باتساق تام في كامل الواجهة — في البطاقات (`7 تصنيفات فرعية`)، وفي أرقام الخطوات، وفي المولَّد برمجياً (`${index + 1}`). لا يوجد أي خلط مع الأرقام العربية-الهندية. وحزمة الخطوط (Tajawal للنص، Cairo للعناوين) اختيار مناسب لواجهة عربية.

**المشكلة الأولى — البحث يفشل على الكتابة العربية الشائعة.** الفلترة تعتمد `toLowerCase()` وحدها، وهي عملية بلا أثر على العربية:

```js
const results = allSearchableItems.filter(item =>
    item.name.toLowerCase().includes(query) ||
    item.category.toLowerCase().includes(query) ||
    item.subcategory.toLowerCase().includes(query)
);
```

لا يوجد أي تطبيع (normalization) للهمزات أو التاء المربوطة أو التشكيل. النتائج المقاسة فعلياً على بيانات المشروع:

| ما يكتبه المستخدم | عدد النتائج |
|---|---|
| `أحوال` | 37 |
| `احوال` | **0** |
| `أجرة` | 7 |
| `اجرة` | **0** |

كتابة الهمزة اختيارية عملياً لدى معظم المستخدمين العرب على لوحات مفاتيح الجوال — هذا ليس حالة حدّية نادرة، بل السلوك الأغلب.

ويتضاعف الأثر بسبب تضارب رسم الألف المقصورة داخل البيانات ذاتها (المذكور في 2.8): البحث عن `دعاوى` يعطي 154 نتيجة، والبحث عن `دعاوي` يعطي 38 — مجموعتان منفصلتان لنفس الكلمة.

**الإصلاح المقترح:**

```js
// قديم
const query = document.getElementById('search-input').value.trim().toLowerCase();
const results = allSearchableItems.filter(item =>
    item.name.toLowerCase().includes(query) || ...
);
```

```js
// جديد
const normalizeAr = s => s
    .replace(/[ً-ْـ]/g, '')   // تشكيل وتطويل
    .replace(/[أإآٱ]/g, 'ا')
    .replace(/ى/g, 'ي')
    .replace(/ة/g, 'ه')
    .toLowerCase();

const query = normalizeAr(document.getElementById('search-input').value.trim());
const results = allSearchableItems.filter(item =>
    normalizeAr(item.name).includes(query) ||
    normalizeAr(item.category).includes(query) ||
    normalizeAr(item.subcategory).includes(query)
);
```

**المشكلة الثانية — خصائص فيزيائية بدل منطقية.** لا يحتوي الملف على أي `margin-inline` أو `padding-inline`، ويعتمد كلياً على `right`/`left`:

```css
.case-type-item:hover {
    background: rgba(27,94,32,0.05);
    padding-right: 2rem;
}
```

```css
.step::before {
    position: absolute;
    right: 24px;
    ...
}
```

في مستند RTL خالص تُعطي هذه النتيجة البصرية الصحيحة اليوم، لكنها تعني أن إضافة نسخة إنجليزية مستقبلاً تتطلّب إعادة كتابة كل هذه القواعد، وأن أي عنصر يُعرَض داخل سياق `dir="ltr"` سينكسر فوراً.

```css
/* قديم */
.case-type-item:hover { padding-right: 2rem; }
```

```css
/* جديد */
.case-type-item:hover { padding-inline-start: 2rem; }
```

**المشكلة الثالثة — اتجاه الأسهم يعتمد على mirroring المتصفح.** زر الرجوع يستخدم `→` وأسهم القوائم تستخدم `←`:

```html
<button class="back-btn" onclick="showCategories()">
    <span>→</span>
    العودة للتصنيفات
</button>
```

المحرفان U+2192 و U+2190 يحملان الخاصية `Bidi_Mirrored=Yes`، أي أن انعكاسهما داخل فقرة RTL متروك لمحرّك العرض ويختلف بين المتصفحات والمنصات. الاعتماد على هذا السلوك لإيصال معنى «رجوع» مخاطرة غير ضرورية — الأفضل SVG صريح أو `transform: scaleX(-1)` مضبوط يدوياً.

**التقييم:** المشكلتان الأولى والثانية `مؤكد`؛ مشكلة انعكاس الأسهم `محتمل` (تحتاج تأكيداً بصرياً على متصفحات متعددة).

**المشكلة الرابعة — لا يوجد تاريخ ولا تعامل مع التقويم.** لا تاريخ هجري ولا ميلادي في الواجهة، ولا حقل «آخر تحديث» — وهو نقص جوهري في دليل قانوني يتغيّر محتواه بتغيّر الأنظمة والخدمات. أُدرجه هنا لأنه جزء من تجربة المستخدم العربي المتوقَّعة لهذا النوع من المحتوى.

---

### 3.5 إمكانية الوصول — 2/10

هذه أضعف فئة في المشروع، والنتائج التالية كلها من فحص مباشر للملف:

| ما تم البحث عنه | عدد المرات في `index.html` |
|---|---|
| `:focus` أو `:focus-visible` | **0** |
| `aria-` (أي سمة) | **0** |
| `role=` | **0** |
| `<label` | **0** |
| `tabindex` | **0** |
| `prefers-reduced-motion` | **0** |

**أولاً: التطبيق غير قابل للتشغيل بلوحة المفاتيح.** جميع البطاقات القابلة للنقر عناصر `<div>` عادية:

```html
<div class="category-card" onclick="showCompletionDetails('heirs')">
    <div class="category-icon icon-blue">📜</div>
    <h3>الورثة</h3>
```

وكذلك عناصر القوائم المولَّدة برمجياً:

```js
const itemEl = document.createElement('div');
itemEl.className = 'case-type-item';
itemEl.onclick = () => showCaseTypeDetails(item, subCatName, category.title);
```

بلا `tabindex` ولا `role="button"` ولا معالجة لمفتاحَي Enter/Space. عملياً: قسما «الإنهاءات» و«الخدمات الإلكترونية» بالكامل، وكل عناصر الدعاوى الـ 191، لا يمكن الوصول إليها إلا بالفأرة أو باللمس.

**ثانياً: لا يوجد أي مؤشّر تركيز مرئي.** صفر قواعد `:focus` تعني أن مستخدم لوحة المفاتيح لا يرى أين هو حتى على الأزرار الحقيقية (`.nav-tab`, `.back-btn`)، لأن `.nav-tab { border: none; }` مع أنماط `:hover` فقط.

**ثالثاً: نافذة الـ modal بلا دلالات ولا حبس تركيز.**

```html
<div class="modal-overlay" id="modal-overlay" onclick="closeModal(event)">
    <div class="modal" onclick="event.stopPropagation()">
```

لا `role="dialog"` ولا `aria-modal="true"` ولا `aria-labelledby`. عند الفتح لا يُنقل التركيز إلى النافذة، وعند الإغلاق لا يعود إلى العنصر المُفعِّل، ولا يوجد قفل تمرير للخلفية. مفتاح Escape يعمل — وهي النقطة الوحيدة الصحيحة هنا:

```js
if (e.key === 'Escape') {
    closeModal();
}
```

**رابعاً: حقل البحث وزرّه بلا اسم متاح.**

```html
<input type="text" id="search-input" placeholder="ابحث عن نوع الدعوى أو الخدمة..." oninput="performSearch()">
<button onclick="performSearch()">🔍</button>
```

الـ `placeholder` ليس بديلاً عن `<label>`، والزر يُقرأ بقارئ الشاشة كاسم الإيموجي فقط.

**خامساً: تسلسل العناوين مكسور.** قسم «الدعاوى القضائية» — وهو القسم الافتراضي المفتوح — لا يحتوي `<h2>` إطلاقاً، فينتقل المستند من `<h1>` في الترويسة مباشرةً إلى `<h3>` في البطاقات. القسمان الآخران (`services`, `steps`) يحتويان `<h2>`، فالتفاوت غير مقصود.

**سادساً: أهداف اللمس أصغر من الحد الموصى به.** زر إغلاق النافذة `36×36` بكسل، وتبويبات التنقّل على الشاشات الصغيرة:

```css
@media (max-width: 380px) {
    .nav-tab {
        padding: 0.5rem 0.6rem;
        font-size: 0.75rem;
    }
}
```

ينتج ارتفاعاً يقارب 38 بكسل — دون حد 44 بكسل.

**سابعاً: لا احترام لـ `prefers-reduced-motion`** رغم وجود `fadeIn` و `modalIn` و `transform` على كل بطاقة.

**ثامناً: أيقونة SVG في الترويسة بلا `aria-hidden`** فتُعلن كصورة بلا وصف.

**التقييم:** كل ما سبق `مؤكد` بالفحص المباشر.

**أسرع إصلاح جذري:**

```html
<!-- قديم -->
<div class="category-card" onclick="showCompletionDetails('heirs')">
```

```html
<!-- جديد -->
<button type="button" class="category-card" onclick="showCompletionDetails('heirs')">
```

```css
/* جديد — يُضاف مرة واحدة */
:focus-visible {
    outline: 3px solid var(--secondary);
    outline-offset: 2px;
}
.category-card { text-align: inherit; font: inherit; width: 100%; }

@media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
        animation-duration: 0.01ms !important;
        transition-duration: 0.01ms !important;
    }
    html { scroll-behavior: auto; }
}
```

---

### 3.6 تغذية راجعة للمستخدم — 4/10

الموجود صحيح ضمن حدوده: حالة «لا نتائج» وحالة «استعلام قصير» كلتاهما معالَجتان صراحةً:

```js
if (query.length < 2) {
    resultsContainer.innerHTML = '<p style="...">أدخل كلمتين على الأقل للبحث</p>';
    return;
}
```

```js
if (results.length === 0) {
    resultsContainer.innerHTML = '<p style="...">لم يتم العثور على نتائج</p>';
    return;
}
```

ملاحظة دقيقة: نص الرسالة يقول «أدخل **كلمتين** على الأقل» بينما الشرط يفحص **حرفين** (`query.length < 2`) — الرسالة لا تصف السلوك الفعلي.

ما ينقص:

- **الحالة الابتدائية فارغة تماماً:** عند فتح تبويب البحث لأول مرة، `#search-results` فارغ بلا أي إرشاد — الرسالة تظهر بعد الكتابة فقط.
- **لا حالات تحميل ولا تأكيد على إجراءات مدمّرة** — وهذا صحيح، لأن التطبيق للقراءة فقط ولا يوجد فيه ما يُدمَّر. لذا لا أخصم عليها.
- **لا إخلاء مسؤولية.** التطبيق يستخدم اللون الأخضر المؤسسي وشعار الميزان وعنوان «نظام ناجز - وزارة العدل» في الترويسة، دون أي توضيح أنه عمل غير رسمي وغير تابع للوزارة، ودون تاريخ لآخر مطابقة للمحتوى. هذه أهم فجوة تواصل في المشروع.

**الإصلاح المقترح:**

```html
<!-- قديم -->
<footer class="footer">
    <p>دليل تقديم الدعاوى القضائية عن طريق نظام ناجز</p>
    <p style="margin-top: 0.5rem;">إعداد: فيصل بن عائض بن سالم المزيني</p>
</footer>
```

```html
<!-- جديد -->
<footer class="footer">
    <p>دليل تقديم الدعاوى القضائية عن طريق نظام ناجز</p>
    <p style="margin-top: 0.5rem;">إعداد: فيصل بن عائض بن سالم المزيني</p>
    <p style="margin-top: 1rem;">
        هذا دليل استرشادي غير رسمي وغير تابع لوزارة العدل أو منصة ناجز،
        ولا يُغني عن الاستشارة القانونية. آخر مطابقة للمحتوى: 2026-08-03.
        المصدر المعتمد هو منصة ناجز الرسمية.
    </p>
</footer>
```

---

### متوسط فئة UX

`(7 + 5 + 7 + 6 + 2 + 4) ÷ 6 = 31 ÷ 6 =` **5.2 / 10**

---

## 4. المراجعة الأمنية

**نموذج التهديد:** صفحة ثابتة على GitHub Pages، بلا backend، بلا مصادقة، بلا قاعدة بيانات، بلا `localStorage`، وبلا أي استدعاء `fetch` (تم التأكد: صفر مثيل لكل من `fetch(` و `localStorage`). المدخل الوحيد من المستخدم هو حقل البحث، وكل البيانات المعروضة مكتوبة يدوياً داخل الملف من قِبل المؤلف. سطح الهجوم شبه معدوم، وأصنّف الشدّة على هذا الأساس لا على مطابقة الأنماط.

### 4.1 الأسرار وبيانات الاعتماد — 10/10

**لا توجد ملاحظات.** لا مفاتيح، ولا رموز، ولا `.env`، ولا أي بيانات اعتماد في أيٍّ من الملفين. عنوان البريد في README (`abo.saleh.g@gmail.com`) هو بيانات تواصل معلنة عمداً، لا تسريب.

---

### 4.2 الحقن (XSS، تجاوز المسارات، حقن الأوامر) — 6/10

**أولاً — ما ليس ثغرة، وأوضّحه لأن مطابقة الأنماط تُخطئ فيه:** استعلام البحث **لا يُعاد عرضه** في الصفحة إطلاقاً. يُستخدم للفلترة فقط، وما يُطبع هو عدد النتائج والبيانات الثابتة:

```js
html = `<p style="...">تم العثور على ${results.length} نتيجة</p>` + html;
```

لذلك **لا يوجد XSS قابل للاستغلال من مدخلات المستخدم في هذا المشروع.**

**ثانياً — النمط الهشّ الفعلي:** بناء معالج `onclick` كسلسلة نصية داخل `innerHTML`، مع حقن قيم داخل علامات اقتباس مفردة:

```js
html += `
    <div class="case-type-item" onclick="showSearchResultDetail('${item.name}', '${item.subcategory}', '${item.category}')">
        <span class="case-type-number">${index + 1}</span>
```

`item.name` مصدره بيانات المؤلف الثابتة فقط، لذلك **الشدّة منخفضة 🔵** في الوضع الحالي. لكن العيب حقيقي وله أثر عملي فوري: أي اسم دعوى يحتوي فاصلة عليا (`'`) — وهو محتمل تماماً عند إضافة بيانات جديدة — يكسر المعالج ويجعل العنصر غير قابل للنقر بلا أي رسالة خطأ. والأخطر أن هذا النمط يتحوّل إلى ثغرة `حرج` لحظة تحميل البيانات من مصدر خارجي (JSON، CMS، API)، وهو أول تطوّر متوقّع لهذا المشروع.

نفس الملاحظة تنطبق على `showCaseTypeDetails` التي تحقن `caseName` و `mainCategory` مباشرةً في `innerHTML`:

```js
<li>حدد التصنيف: ${mainCategory}</li>
<li>اختر نوع الدعوى: ${caseName}</li>
```

**التصنيف:** CWE-79 (Improper Neutralization of Input During Web Page Generation) — تصنيف بنيوي لا استغلالي هنا، ومرتبط بـ CWE-116 (Improper Encoding or Escaping of Output).

**الإصلاح — إسناد المعالج بدل كتابته نصاً:**

```js
// قديم
html += `<div class="case-type-item" onclick="showSearchResultDetail('${item.name}', '${item.subcategory}', '${item.category}')">...`;
resultsContainer.innerHTML = html;
```

```js
// جديد — بناء العناصر برمجياً وربط الحدث بالمرجع
const row = document.createElement('div');
row.className = 'case-type-item';
row.append(numSpan, nameWrap, arrowSpan);            // نصوص عبر textContent
row.addEventListener('click', () =>
    showCaseTypeDetails(item.name, item.subcategory, item.category));
resultsContainer.appendChild(row);
```

هذا يزيل الفاصلة العليا كمصدر أعطال، ويلغي الحاجة إلى الدالة الوسيطة `showSearchResultDetail` المذكورة في 2.3.

**لا ينطبق:** حقن SQL، حقن الأوامر، تجاوز المسارات، حقن القوالب — لا يوجد خادم ولا نظام ملفات ولا محرّك قوالب.

---

### 4.3 التحقق من المدخلات والتنقية — 7/10

المدخل الوحيد هو نص البحث، ويخضع لتقييد طول أدنى وتشذيب:

```js
const query = document.getElementById('search-input').value.trim().toLowerCase();
if (query.length < 2) { ... return; }
```

لا يُمرَّر إلى أي `eval` ولا `innerHTML` ولا عنوان URL. لا حد أقصى للطول، لكن الأثر الأسوأ الممكن هو فلترة بلا نتائج على 220 عنصراً — غير ذي دلالة أمنية.

**التقييم:** كافٍ لنموذج التهديد الحالي.

---

### 4.4 المصادقة والتفويض — لا ينطبق

لا يوجد نظام مصادقة ولا جلسات ولا صلاحيات في المشروع. مستبعد من حساب متوسط الفئة.

---

### 4.5 التعامل مع البيانات الحساسة — 9/10

لا يجمع التطبيق ولا يخزّن ولا يرسل أي بيانات مستخدم: صفر `localStorage`، صفر `sessionStorage`، صفر `fetch`، صفر نماذج إرسال، ولا تسجيل (`console.log`) لأي شيء.

**الملاحظة الوحيدة — تسريب بيانات إلى طرف ثالث (شدّة: منخفض 🔵):**

```html
<link href="https://fonts.googleapis.com/css2?family=Tajawal:..." rel="stylesheet">
```

كل زائر يُصدر طلباً إلى خوادم Google يحمل عنوان IP ووكيل المتصفح والـ Referer. لموقع يتصفّحه مواطنون بحثاً عن معلومات قضائية، استضافة الخط ذاتياً تُلغي هذا الأثر وتحسّن الأداء في الوقت نفسه.

**التقييم:** `مؤكد`.

---

### 4.6 صحة الاعتماديات — 8/10

لا توجد اعتماديات JavaScript إطلاقاً — لا `package.json`، ولا `node_modules`، ولا مكتبة محمّلة من CDN. هذا يُلغي فئة كاملة من مخاطر سلسلة التوريد، ويستحق التقدير صراحةً.

الاعتماد الخارجي الوحيد هو ورقة أنماط Google Fonts، وهي غير مثبَّتة على إصدار ولا يمكن حمايتها بـ Subresource Integrity لأن Google تُرجع محتوى متغيّراً حسب وكيل المتصفح. أثر تعطّلها انحسار إلى `sans-serif` — ضعيف الأثر لكنه اعتماد على طرف خارجي في مسار العرض الحرج.

**لا أذكر أي CVE** — لا يوجد ما يُفحص.

**الإصلاح المقترح:** استضافة ملفَّي `woff2` محلياً في المستودع وتعريفهما بـ `@font-face`، مع مكدّس بديل عربي صريح بدل `sans-serif` وحده:

```css
/* قديم */
body { font-family: 'Tajawal', sans-serif; }
```

```css
/* جديد */
body { font-family: 'Tajawal', 'Segoe UI', 'Noto Sans Arabic', 'Geeza Pro', Tahoma, sans-serif; }
```

---

### 4.7 CORS و CSP ورؤوس الأمان — 3/10

**لا توجد أي رؤوس أمان** — صفر مثيل لـ `http-equiv` في الملف. لا `Content-Security-Policy`، ولا `Referrer-Policy`، ولا `X-Content-Type-Options`. GitHub Pages لا يسمح بتخصيص رؤوس HTTP، لكن وسم `<meta>` متاح ويغطي الجزء الأهم.

الشدّة هنا **متوسط 🟡** لا أعلى: بلا مدخلات غير موثوقة، دور CSP وقائي (دفاع في العمق) لا علاجي.

الوضع الحالي يستخدم `<style>` و `<script>` مضمّنين و 22 معالج `onclick` مضمّناً، ما يفرض `'unsafe-inline'` — لذا السياسة أدناه خطوة أولى واقعية، وقيمتها الحقيقية في `default-src 'self'` و `frame-ancestors 'none'`:

```html
<!-- جديد — يُضاف داخل <head> -->
<meta http-equiv="Content-Security-Policy" content="
    default-src 'self';
    style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
    font-src 'self' https://fonts.gstatic.com;
    script-src 'self' 'unsafe-inline';
    img-src 'self' data:;
    frame-ancestors 'none';
    base-uri 'none'">
<meta name="referrer" content="strict-origin-when-cross-origin">
```

نقل معالجات `onclick` إلى `addEventListener` (المذكور في 2.2) يسمح لاحقاً بإسقاط `'unsafe-inline'` من `script-src`، وهو المكسب الأمني الحقيقي.

**التقييم:** `مؤكد`.

---

### متوسط الفئة الأمنية

`(10 + 6 + 7 + 9 + 8 + 3) ÷ 6 = 43 ÷ 6 =` **7.2 / 10**
(«المصادقة والتفويض» مستبعدة كـ `لا ينطبق`)

---

## 5. الدرجة الإجمالية المرجّحة

**الملف التعريفي المختار: C — أداة من ملف واحد، بلا backend وبلا تعامل مع بيانات مستخدم.**

**سبب الاختيار:** المشروع ملف HTML واحد يُفتح محلياً أو يُخدَم كصفحة ثابتة؛ لا خادم، ولا مصادقة، ولا تخزين، ولا استقبال لبيانات من أي مصدر غير موثوق. قيمته كلها في عرض المحتوى، ولذلك يُرفَع وزن UX ويُخفَّض وزن الأمن. الملف A كان سيعطي الأمن 25% رغم انعدام سطح الهجوم عملياً، والملف D غير منطبق تماماً.

| الناحية | الدرجة | الوزن | المساهمة |
|---|---|---|---|
| تقنية وهندسية | 4.3/10 | 40% | 1.72 |
| UX/UI | 5.2/10 | 40% | 2.08 |
| أمن سيبراني | 7.2/10 | 20% | 1.44 |
| **الإجمالي** | | **100%** | **5.24** |

**الحساب التفصيلي:**

- الهندسة: `(3 + 5 + 4 + 4 + 7 + 0 + 5 + 6) ÷ 8 = 4.25` → `4.25 × 0.40 = 1.70`
- UX: `(7 + 5 + 7 + 6 + 2 + 4) ÷ 6 = 5.17` → `5.17 × 0.40 = 2.07`
- الأمن: `(10 + 6 + 7 + 9 + 8 + 3) ÷ 6 = 7.17` → `7.17 × 0.20 = 1.43`
- **المجموع: 1.70 + 2.07 + 1.43 = 5.20**

### **الدرجة النهائية: 5.2 / 10**

هذه هي المنطقة المتوقَّعة لمشروع شخصي وظيفي بحسب مرساة التقييم: يعمل، ويؤدي غرضه، ويحمل ديناً تقنياً مرئياً. الأمن يرفع المتوسط لأن المشروع لا يفعل شيئاً خطراً — لا لأنه محصَّن.

---

## 6. خطة الإصلاح المرتبة بالأولوية

| # | الأولوية | الناحية | المشكلة | الملف | الحل المقترح | الجهد | التأثير بعد الحل |
|---|---|---|---|---|---|---|---|
| 1 | حرج 🔴 | UX | كل العناصر التفاعلية `<div onclick>` بلا `tabindex` أو `role` — التطبيق غير قابل للتشغيل بلوحة المفاتيح | `index.html` | تحويل `.category-card` و `.service-item` إلى `<button type="button">`، وإضافة `role="button"` + `tabindex="0"` + معالجة Enter/Space لعناصر `.case-type-item` المولَّدة | ساعة | الوصول من 2 إلى ≈ 6 |
| 2 | حرج 🔴 | UX | صفر قواعد `:focus` — مستخدم لوحة المفاتيح لا يرى موقعه | `index.html` (`<style>`) | إضافة قاعدة `:focus-visible` عامة بـ `outline: 3px solid var(--secondary)` | دقائق | شرط لازم لاكتمال البند 1 |
| 3 | حرج 🔴 | هندسة | 8 بطاقات تعلن أعداداً مخالفة للبيانات («25+ خدمة» أمام 11 عنصراً) | `index.html` | استبدال الأعداد الثابتة بـ `data-count-for` تُملأ من البيانات عند التحميل | ساعة | إزالة محتوى مضلّل من دليل قانوني |
| 4 | حرج 🔴 | هندسة | 99 عنصراً (31% من المحتوى) خارج فهرس البحث لأن `serviceData` محبوسة داخل دالة | `index.html` | نقل `serviceData` إلى نطاق الوحدة وإضافة حلقة بناء الفهرس الثالثة | دقائق | تحقيق وعد README؛ تغطية البحث من 69% إلى 100% |
| 5 | حرج 🔴 | UX | البحث يفشل على الكتابة بلا همزة: `احوال` = 0 نتيجة، `أحوال` = 37 | `index.html` (`performSearch`) | دالة `normalizeAr` تُطبَّق على الاستعلام وعلى الحقول الثلاثة | دقائق | إصلاح أكثر أنماط البحث شيوعاً لدى المستخدم العربي |
| 6 | مهم 🟠 | UX | لا إخلاء مسؤولية ولا تاريخ تحديث رغم استخدام هوية بصرية شبه رسمية | `index.html` (`.footer`) | فقرة توضح أن الدليل غير رسمي + تاريخ آخر مطابقة | دقائق | إزالة الالتباس بشأن رسمية المصدر |
| 7 | مهم 🟠 | UX | نافذة modal بلا `role="dialog"` ولا حبس تركيز ولا إعادة تركيز عند الإغلاق | `index.html` | إضافة `role="dialog"` + `aria-modal` + `aria-labelledby`، وحفظ العنصر المُفعِّل وإعادة التركيز إليه، وقفل تمرير الخلفية | ساعة | نافذة صالحة للاستخدام بقارئ الشاشة |
| 8 | مهم 🟠 | UX | حقل البحث بلا `<label>` وزرّه بلا اسم متاح (إيموجي فقط) | `index.html` | `<label class="sr-only">` للحقل و `aria-label="بحث"` للزر | دقائق | الحقل الوحيد في التطبيق يصبح معلَناً |
| 9 | مهم 🟠 | هندسة | صفر اختبارات — سبب بقاء البند 3 دون اكتشاف | المستودع | اختبار واحد يقارن كل `.category-count` بعدد مفاتيح البيانات المقابلة، مربوط بـ GitHub Actions | يوم | منع تكرار انحراف البيانات عن العرض |
| 10 | مهم 🟠 | UX | محتوى الـ modal متطابق لكل الدعاوى الـ 191 — لا مستندات ولا محكمة مختصة ولا رسوم | `index.html` (`showCaseTypeDetails`) | توسيع بنية البيانات بحقول `documents` و `court` و `notes` اختيارية، مع الإبقاء على النص العام كحالة افتراضية | أكثر | تحويل الدليل من فهرس إلى مرجع فعلي |
| 11 | مهم 🟠 | UX | صفر وسم `<a>` — لا رابط واحد إلى ناجز في دليل عن ناجز | `index.html` | ربط `najiz.sa` في قسم الخطوات وإضافة روابط في التذييل | دقائق | إزالة نقطة احتكاك واضحة |
| 12 | مهم 🟠 | UX | لا توجد حالة قابلة للمشاركة أو الحفظ؛ إعادة التحميل تعيد للتبويب الأول | `index.html` | توجيه بسيط عبر `location.hash` مع `hashchange` | ساعة | إمكانية مشاركة روابط مباشرة لتصنيف محدد |
| 13 | مهم 🟠 | UX | الطباعة تُنتج التبويب المفتوح فقط لا الدليل كاملاً | `index.html` (`@media print`) | إظهار كل `.section` و `#subcategory-view` عند الطباعة | دقائق | نسخة ورقية قابلة للاستخدام |
| 14 | مهم 🟠 | هندسة | `serviceData` داخل دالة العرض؛ ثلاث دوال modal شبه متطابقة؛ دالة وسيطة `showSearchResultDetail` بلا وظيفة | `index.html` | نقل البيانات لنطاق الوحدة، ودمج بنّائي الـ modal في `openListModal`، وحذف الوسيط | ساعة | تقليص ≈ 40 سطراً مكرراً ومصدر واحد للبيانات |
| 15 | مهم 🟠 | أمن | معالجات `onclick` تُبنى كسلاسل نصية مع حقن قيم بين علامات اقتباس مفردة | `index.html` (`performSearch`) | بناء العناصر عبر `createElement` + `textContent` وربط الحدث بمرجع دالة | ساعة | إزالة عطل الفاصلة العليا ومنع تحوّله لثغرة عند ربط مصدر بيانات خارجي |
| 16 | مهم 🟠 | هندسة | لا يوجد ملف `LICENSE` رغم أن README يعلن ترخيصاً ويدعو للمساهمة | المستودع | إضافة ملف ترخيص صريح (MIT أو CC-BY للمحتوى) وربطه من README | دقائق | إزالة تناقض قانوني في المستودع |
| 17 | تحسين 🟡 | أمن | لا `Content-Security-Policy` ولا `Referrer-Policy` | `index.html` (`<head>`) | وسما `<meta>` كما في 4.7 | دقائق | دفاع في العمق |
| 18 | تحسين 🟡 | أمن/أداء | خط Google خارجي: تسريب IP لكل زائر + دورة اتصال إضافية | `index.html` | استضافة `woff2` محلياً؛ وإن تعذّر فإضافة `preconnect` | ساعة | خصوصية أفضل وعرض أسرع للنص |
| 19 | تحسين 🟡 | UX | لا احترام لـ `prefers-reduced-motion` رغم وجود animations و transforms | `index.html` (`<style>`) | كتلة `@media (prefers-reduced-motion: reduce)` | دقائق | راحة المستخدمين الحساسين للحركة |
| 20 | تحسين 🟡 | UX | أهداف لمس دون 44 بكسل: زر الإغلاق `36×36`، والتبويبات ≈ 38 بكسل عند `380px` | `index.html` | رفع `min-height`/`min-width` إلى `44px` | دقائق | دقة لمس أعلى على الجوال |
| 21 | تحسين 🟡 | UX/RTL | استخدام خصائص فيزيائية (`padding-right`, `right`) في كامل الملف بلا أي خاصية منطقية | `index.html` | الانتقال إلى `padding-inline-start` و `inset-inline-*` (≈ 10 قواعد) | ساعة | جاهزية لأي نسخة LTR مستقبلية |
| 22 | تحسين 🟡 | UX/RTL | اتجاه أسهم `→`/`←` متروك لـ mirroring المتصفح (`Bidi_Mirrored`) | `index.html` | استبدالها بـ SVG صريح أو `transform: scaleX(-1)` مضبوط | دقائق | ثبات الاتجاه عبر المتصفحات |
| 23 | تحسين 🟡 | UX | رسالة «أدخل كلمتين على الأقل» بينما الشرط `query.length < 2` يفحص حرفين | `index.html` | تعديل النص إلى «أدخل حرفين على الأقل» | دقائق | رسالة تصف السلوك الفعلي |
| 24 | تحسين 🟡 | هندسة | 12 سمة `style` مضمّنة + `cssText` من JS تتجاوز نظام الـ tokens | `index.html` | نقلها إلى CSS classes (`.section-title`, `.subcategory-group-header`, `.search-hint`) | ساعة | نظام تصميم واحد لا اثنان |
| 25 | تحسين 🟡 | هندسة/توثيق | README: قسم لقطات شاشة بلا صور، ونسبة عمل متضاربة بين ثلاثة مواضع، وصفر تعليقات في JS | `README.md`, `index.html` | إضافة لقطات فعلية، وتوحيد الإسناد في موضع واحد، وتعليق يوثّق بنية البيانات ومصدر المحتوى | ساعة | توثيق يطابق الواقع |

**ملاحظة:** الجدول يغطي كل الملاحظات المرصودة دون تجميع. البنود 1 و 2 مترابطان وينبغي تنفيذهما معاً.

---

## 7. أسرع المكاسب

خمسة إصلاحات، كلٌّ منها دون 30 دقيقة، بأعلى نسبة أثر إلى جهد في المشروع.

### 7.1 ضمّ 99 عنصراً إلى البحث — دقيقتان

يزيد تغطية البحث من 69% إلى 100%، ويحقّق وعداً معلناً في README.

```js
// قديم — الفهرس يُبنى من مجموعتين فقط
Object.keys(completionData).forEach(key => {
    const comp = completionData[key];
    comp.items.forEach(item => {
        allSearchableItems.push({ name: item, category: comp.title, subcategory: 'إنهاء', type: 'إنهاء' });
    });
});
```

```js
// جديد — يُضاف بعدها مباشرة (بعد نقل serviceData إلى نطاق الوحدة)
Object.keys(completionData).forEach(key => {
    const comp = completionData[key];
    comp.items.forEach(item => {
        allSearchableItems.push({ name: item, category: comp.title, subcategory: 'إنهاء', type: 'إنهاء' });
    });
});

Object.keys(serviceData).forEach(key => {
    const svc = serviceData[key];
    svc.items.forEach(item => {
        allSearchableItems.push({ name: item, category: svc.title, subcategory: 'خدمة إلكترونية', type: 'خدمة' });
    });
});
```

---

### 7.2 تطبيع البحث العربي — 5 دقائق

يحوّل `احوال` من 0 نتيجة إلى 37، و `اجرة` من 0 إلى 7، ويوحّد نتائج `دعاوى`/`دعاوي` المنفصلتين حالياً.

```js
// قديم
const query = document.getElementById('search-input').value.trim().toLowerCase();
...
const results = allSearchableItems.filter(item =>
    item.name.toLowerCase().includes(query) ||
    item.category.toLowerCase().includes(query) ||
    item.subcategory.toLowerCase().includes(query)
);
```

```js
// جديد
const normalizeAr = s => s
    .replace(/[ً-ْـ]/g, '')
    .replace(/[أإآٱ]/g, 'ا')
    .replace(/ى/g, 'ي')
    .replace(/ة/g, 'ه')
    .toLowerCase();

const query = normalizeAr(document.getElementById('search-input').value.trim());
...
const results = allSearchableItems.filter(item =>
    normalizeAr(item.name).includes(query) ||
    normalizeAr(item.category).includes(query) ||
    normalizeAr(item.subcategory).includes(query)
);
```

---

### 7.3 مؤشّر تركيز مرئي + احترام تقليل الحركة — 3 دقائق

سطران يرفعان أهم بندين قابلين للإصلاح فوراً في فئة الوصول.

```css
/* جديد — يُضاف بعد كتلة :root مباشرة */
:focus-visible {
    outline: 3px solid var(--secondary);
    outline-offset: 2px;
    border-radius: 4px;
}

@media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
    html { scroll-behavior: auto; }
}
```

---

### 7.4 اسم متاح لحقل البحث وزرّه — 3 دقائق

```html
<!-- قديم -->
<div class="search-box">
    <input type="text" id="search-input" placeholder="ابحث عن نوع الدعوى أو الخدمة..." oninput="performSearch()">
    <button onclick="performSearch()">🔍</button>
</div>
```

```html
<!-- جديد -->
<div class="search-box">
    <label for="search-input" class="sr-only">ابحث في أنواع الدعاوى والخدمات</label>
    <input type="search" id="search-input" placeholder="ابحث عن نوع الدعوى أو الخدمة..." oninput="performSearch()">
    <button type="button" onclick="performSearch()" aria-label="بحث">🔍</button>
</div>
<div id="search-results" aria-live="polite"></div>
```

```css
/* جديد */
.sr-only {
    position: absolute; width: 1px; height: 1px;
    padding: 0; margin: -1px; overflow: hidden;
    clip: rect(0,0,0,0); white-space: nowrap; border: 0;
}
```

---

### 7.5 إخلاء مسؤولية وتاريخ تحديث — 3 دقائق

أرخص إصلاح في القائمة وأعلاها أثراً على المخاطر: يفصل بوضوح بين هذا الدليل وبين الجهة الرسمية التي يستعير هويتها البصرية.

```html
<!-- قديم -->
<footer class="footer">
    <p>دليل تقديم الدعاوى القضائية عن طريق نظام ناجز</p>
    <p style="margin-top: 0.5rem;">إعداد: فيصل بن عائض بن سالم المزيني</p>
</footer>
```

```html
<!-- جديد -->
<footer class="footer">
    <p>دليل تقديم الدعاوى القضائية عن طريق نظام ناجز</p>
    <p style="margin-top: 0.5rem;">إعداد: فيصل بن عائض بن سالم المزيني</p>
    <p style="margin-top: 1rem; font-size: 0.8rem;">
        دليل استرشادي غير رسمي، غير تابع لوزارة العدل أو منصة ناجز، ولا يُغني عن الاستشارة القانونية.
        آخر مطابقة للمحتوى: 2026-08-03 — المصدر المعتمد:
        <a href="https://www.najiz.sa/" style="color: var(--secondary);">najiz.sa</a>
    </p>
</footer>
```

---

## 8. ما لم تتم مراجعته

**نسبة التغطية: ≈ 100% من الكود المصدري، مع القيود التالية على طبيعة الفحص.**

| المجال | الحالة | السبب |
|---|---|---|
| `index.html` | قُرئ بالكامل (1486 سطراً) | — |
| `README.md` | قُرئ بالكامل (143 سطراً) | — |
| اختبارات | **غير موجودة** | لا يوجد أي ملف اختبار في المستودع |
| CI/CD | **غير موجود** | لا يوجد مجلد `.github/` |
| إعدادات linter/formatter | **غير موجودة** | لا يوجد أي ملف إعداد |
| `.gitignore` / `LICENSE` / `.env.example` | **غير موجودة** | لا شيء يُراجَع |
| ملفات ثنائية أو حِزَم مبنية | لا توجد | المستودع نصي بالكامل |

**ما لم يُتحقّق منه — بصراحة:**

1. **التنفيذ الفعلي في المتصفح.** المراجعة تحليل ساكن للكود؛ لم أشغّل الصفحة ولم ألتقط لقطات. كل ملاحظات التخطيط والانعكاس البصري وتناسق العرض مستنتجة من CSS، لا مشاهَدة. مسألة انعكاس أسهم `→`/`←` (3.4) على وجه الخصوص تحتاج تأكيداً بصرياً على Chrome و Safari و Firefox، ولهذا صُنّفت `محتمل`.

2. **نسب التباين محسوبة رياضياً لا مقيسة.** حسبت قيم WCAG من ألوان CSS المصرّحة، وجاءت المجموعات الأساسية سليمة: `--text-secondary #555555` على أبيض ≈ 7.4:1، وعلى `--bg-cream #FAFAF5` ≈ 7.2:1؛ و `--primary #1B5E20` على `--bg-cream` ≈ 7.6:1؛ ونص التذييل بعتامة 0.8 على `--primary-dark` ≈ 9.4:1. الاستثناء الوحيد نص الترويسة `rgba(255,255,255,0.9)` فوق التدرّج: مقبول عند مركزه (≈ 7:1 فوق `--primary`) لكنه يهبط إلى ≈ 2.5:1 عند طرفه الفاتح `--primary-light #4CAF50`. لأن النص محاذٍ للمنتصف فالأرجح أنه لا يقع في تلك المنطقة — ولذلك لم أدرجه كملاحظة مستقلة، لكنه يحتاج قياساً على العرض الفعلي لتأكيد ذلك.

3. **دقة المحتوى القانوني نفسه.** لم أراجع صحة أسماء الدعاوى الـ 319 ولا تصنيفاتها ولا مطابقتها لما تعرضه منصة ناجز اليوم. هذا يحتاج مختصاً قانونياً ومقارنة مع المصدر الرسمي، وهو خارج نطاق المراجعة الهندسية. غياب تاريخ «آخر تحديث» في المشروع يجعل هذا التحقق غير ممكن أصلاً من داخل المستودع.

4. **سلوك النشر على GitHub Pages.** لم أتحقّق من إعدادات النشر ولا من الرؤوس التي يُرجعها الخادم فعلياً؛ استنتاجات القسم 4.7 مبنية على القيود المعروفة للمنصة وعلى غياب أي وسم `http-equiv` في الملف.

5. **تاريخ المستودع.** فحصت آخر خمسة commits لتحديد نقطة المراجعة فقط، ولم أراجع السجل الكامل بحثاً عن أسرار مسرّبة سابقاً. بالنظر إلى أن المشروع لم يحتوِ قط على backend أو مفاتيح، احتمال وجود ذلك ضعيف — لكنني لم أفحصه.

**مستوى الثقة في التقرير: عالٍ** بالنسبة للملاحظات الهندسية والأمنية وملاحظات الوصول — جميعها موثّقة باقتباسات مباشرة من الملف، وأرقام البيانات في 2.4 و 3.4 محسوبة بتنفيذ بيانات المشروع برمجياً لا بالعدّ اليدوي. **متوسط** بالنسبة لملاحظات العرض البصري التي تحتاج تشغيلاً فعلياً في المتصفح للتأكيد.
